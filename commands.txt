-- Commands --

# Leer un TXT
data = sc.textFile(file_path)
data = sc.textFile(file_path).cache
# Operaciones de RDD - ACCIONES
data.count() data.first() data.take(5)

# Operaciones de RDD - TRANSFORMACIONS
data.filter(lambda s: 'juan' in s.lower())

# Leer un CSV
